const { StyleSheet } = require("react-native");

export const styles = StyleSheet.create({
  container: { margin: 5 },
  likes: {
    fontWeight: "bold",
  },
  caption: {},
  postedTime: {
    color: "#8c8c8c",
  },
});
